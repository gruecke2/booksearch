import React, { Component } from 'react';
import './style.css';

class Saved extends Component {
    state = {
        savedBooks: []
    }

  render() {
    return (
      <div className="Saved">
        
      </div>
    );
  }
}

export default Saved;
